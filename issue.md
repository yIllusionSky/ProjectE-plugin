# 功能：升级到 1.21.11 并优化转换桌交互体验

## 背景

当前 ProjectE 插件在转换桌相关交互上存在一些比较明显的使用问题，已经影响日常操作体验：

- 部分操作完成后会自动关闭当前 GUI，连续操作时比较卡手，需要反复重新打开界面。
- 卖出物品会自动加入已学习列表，导致很多本来只是清理掉的无用物品、垃圾物品也进入购买页。
- 当前没有移除已学习物品的功能，一旦误学或学习内容被污染，无法直接在 GUI 内删除。
- 项目当前也需要同步升级到 `1.21.11`，但本次仅做最小兼容升级，不调整 `plugin.yml` 中的 `api-version`。

## 需求目标

本次需求属于功能改造与最小版本升级，目标如下：

- 构建依赖升级到 `spigot-api 1.21.11-R0.1-SNAPSHOT`
- 保持 `plugin.yml` 中 `api-version: '1.16'` 不变
- 优化转换桌交互流程，减少重复关界面的打断感
- 调整学习逻辑，避免卖出即学习
- 为已学习物品补充移除能力
- 增加仅重置转换桌数据库的命令入口

## 现有问题

### 1. 操作完成后 GUI 自动关闭

当前卖出、学习、部分确认操作会关闭 Inventory，再重新打开或让玩家手动重新进入，实际体验比较生硬。

期望：

- 操作完成后尽量原地更新当前 Inventory
- 不再默认关闭 GUI 再重开

### 2. 卖出物品会自动学习

当前卖出行为会把物品自动加入已学习列表，这会让很多本来只是打算处理掉的低价值或无用物品进入购买页。

这类行为不符合使用预期。

期望：

- 卖出不再自动学习
- 学习页作为唯一学习入口
- 只有玩家明确执行“学习”的物品，才进入已学习列表

### 3. 缺少已学习物品移除入口

当前学习记录一旦写入，缺少对应的删除手段，误操作后无法在 GUI 中直接修正。

期望：

- Java 版支持 `Shift + 左键` 移除学习物品
- 同时增加一个显式“移除模式”按钮，兼容普通用户和基岩 / Geyser 用户
- 移除后当前槽位应立即失效或置灰，避免本页继续购买该物品
- 暂不要求整页即时强刷新，重新进入页面后列表自然更新即可

### 4. 缺少仅清理转换桌数据库的命令

当前没有默认的“清配置/清数据库”命令，但也缺少一个只针对转换桌学习数据库的安全重置入口。

期望：

- 在 `projecte` 主命令下增加一个仅 OP 可用的子命令，例如 `/projecte resetdb`
- 该命令只清理 `plugins/ProjectE/gui/transmutation.db`
- 不删除 `config.yml`、`command.yml`、`devices.yml`、语言文件、映射文件、资源包等其他文件
- 执行后关闭数据库连接、删除 DB、重新初始化，并恢复插件运行所需状态

## 实现范围

本次 issue 范围包括：

- 最小升级到 `1.21.11`
- 新增 `resetdb` 命令、权限节点、帮助文本和语言文本
- 移除卖出自动学习逻辑
- 为购买页补充“移除已学习物品”能力
- 将卖出 / 学习 / 相关确认操作改为原地更新 GUI

## 非目标

以下内容不在本次 issue 范围内：

- 不做大规模代码格式化
- 不做纯样式类重构
- 不包含 GitHub Actions / CI / 自动发版流程改造
- 不修改 `plugin.yml` 的 `api-version`
- 不做全量配置清空，只处理转换桌数据库

## 验收标准

- 插件可在 `1.21.11` 环境正常构建与启动
- 卖出物品后不会自动加入学习列表
- 学习页仍然可以正常学习物品
- 已学习物品支持通过 `Shift + 左键` 或 GUI 移除模式删除
- 移除后当前槽位立即不可继续购买
- 卖出 / 学习 / 确认操作后 GUI 不再自动关闭
- `/projecte resetdb` 只清理转换桌数据库，不影响其他配置与资源文件
